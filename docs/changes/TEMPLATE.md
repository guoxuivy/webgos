# <变更标题>

- 日期：
- 负责人：
- 关联分支 / PR：

## 背景与问题

<当前发生了什么问题，证据是什么。Bug 要写稳定复现步骤。不要粘贴敏感原文。>

## 范围与非目标

**范围内**：

- 

**非目标**：

- 

## 验收场景

**正常路径**：Given <前提>，When <动作>，Then <预期结果>

**拒绝路径**：Given <越权/非法/超限>，When <动作>，Then <明确拒绝且不写入>

**恢复路径**：Given <部分失败/中断>，When <重试/修正>，Then <可恢复且不重复写入>

## 影响面

- [ ] API 契约（字段 / 错误码 / Swagger）
- [ ] 数据库结构与迁移
- [ ] 权限与鉴权（新增路由即新增 RBAC 权限点）
- [ ] 缓存（增删改后是否需 `DeleteByPrefix`）
- [ ] 前端 / 调用方适配
- [ ] 其他：

## 回滚方案

<具体动作：迁移 down 脚本 / 配置开关 / 回退 commit。禁止只写「重新发布上一版」。>

## 验证结果

- 定向测试：
- `make check`：
- 备注（未执行项必须写明，不得把 skip 写成 passed）：
